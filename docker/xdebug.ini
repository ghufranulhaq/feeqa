; Local development only (dev stage). Off by default; enable per-request with
; the XDEBUG_TRIGGER cookie/header, or set xdebug.mode via .env-driven override
; if you want it on for every request.
xdebug.mode = off
xdebug.client_host = host.docker.internal
xdebug.start_with_request = trigger
